namespace Arcane_Aegis.Entities
{
    /// <summary>View for a hostile mob (mirrors the server's <c>Monster</c> — a sibling of Npc, not a subclass).
    /// Living entity via HumanoidView (health bar). Hook nameplate/aggro FX here later.</summary>
    public class MonsterView : HumanoidView
    {
    }
}
