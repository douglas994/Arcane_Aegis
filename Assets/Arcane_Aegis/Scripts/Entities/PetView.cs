namespace Arcane_Aegis.Entities
{
    /// <summary>View for a player-owned ally (mirrors the server's <c>Pet : Entity</c> — NOT a humanoid). HP bar +
    /// death via the <see cref="CombatantVitals"/> component (composition).</summary>
    public class PetView : EntityView
    {
    }
}
